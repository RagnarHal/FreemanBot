{
  "name": "freemanbot",
  "version": "1.0.0",
  "description": "",
  "main": "./src/main.js",
  "scripts": {
    "babel-node": "babel-node",
    "build": "rimraf dist/ && babel ./src --out-dir dist/ --ignore ./node_modules && cp ./src/config.json ./dist/config.json",
    "start": "yarn build && cross-env NODE_ENV=production node ./dist/main.js",
    "dev": "cross-env NODE_ENV=development nodemon --exec yarn babel-node -- ./src/main.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "lint": "eslint ./src",
    "precommit": "eslint ./src"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "discord.js": "^11.3.0",
    "winston": "^3.0.0-rc1"
  },
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-preset-es2015": "^6.24.1",
    "cross-env": "^5.1.3",
    "dotenv": "^4.0.0",
    "eslint": "^4.16.0",
    "eslint-config-node": "^2.0.0",
    "husky": "^0.14.3",
    "nodemon": "^1.14.11"
  }
}
